type Query {
	getAllUsers: [User]
	getUserById (userId: String): User
}

type Mutation {
  updateUser (input: inputUpdateUser): User
  deleteUser (userId: String): ResponseResult
}

type ResponseResult {
  requestResolved: Boolean!
  error: String
}

input inputUpdateUser {
  _id: String
  name: String
  email: String
}